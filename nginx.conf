server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    location = /imagelab {
        return 301 /imagelab/;
    }

    location /imagelab/ {
        rewrite ^/imagelab/(.*)$ /$1 break;
        try_files $uri $uri/ /index.html;
    }
}
